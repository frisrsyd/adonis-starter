<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="shortcut icon" href="{{asset('resources/images/adonis.jpg')}}" type="image/x-icon">
  <title> {{{ await $slots.title()}}} </title>
    
  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body>
  @layouts.header()
  @end
    <h1 class="text-3xl"> {{{ await $slots.title()}}} </h1>
    {{{ await $slots.body() }}}
</body>
</html>